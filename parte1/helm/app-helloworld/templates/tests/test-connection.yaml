apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-helloworld.fullname" . }}-test-connection"
  labels:
    {{- include "app-helloworld.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-helloworld.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
